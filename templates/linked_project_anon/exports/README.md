# exports — [PROJECT_NAME]

Generated exports from TricorderKit pipelines.

**Rule**: never export raw private data. Anonymize with `security-audit-cli` before any public push.

Formats: CSV, JSON, JSONL, Markdown.

---

*linked_project template — TricorderKit v0.9*
