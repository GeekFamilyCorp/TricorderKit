# requirements-dev.txt - dependances pour la suite de tests (CI + local)
# Cure depuis l'environnement reel (pip freeze), tiers uniquement :
#   - exclut les installs editables a chemin personnel / composants prives ;
#   - exclut les paquets Windows-only (pywin32, mitmproxy*, pydivert) ;
#   - epingle aux versions validées localement.
# Cible : Python 3.11. Tests reseau exclus en CI via -m "not live".

# --- Test runner ---
pytest==9.0.3
pytest-cov==7.1.0

# --- Core libs (config, CLI, schemas) ---
PyYAML==6.0.3
typer==0.25.1
click==8.4.1
click-option-group==0.5.9
rich==14.3.3
tabulate==0.10.0
jsonschema==4.25.1
jsonpatch==1.33
python-dotenv==1.2.1
pydantic==2.12.5
pydantic-settings==2.14.1
Jinja2==3.1.6
psutil==7.2.2
python-dateutil==2.9.0.post0

# --- HTTP / sources ---
requests==2.33.0
requests-cache==1.3.1
httpx==0.28.1
feedparser==6.0.12
beautifulsoup4==4.14.3
RapidFuzz==3.14.5

# --- Workflows / orchestration / observability ---
temporalio==1.27.2
langgraph==1.2.0
langchain-core==1.4.0
langfuse==2.60.10
tenacity==9.1.4
croniter==6.0.0

# --- LLM clients (importes par certains modules) ---
openai==2.24.0
