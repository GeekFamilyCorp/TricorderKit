{
  "mcpServers": {
    "kintone": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@kintone/mcp-server"],
      "env": {
        "KINTONE_BASE_URL": "${KINTONE_BASE_URL}",
        "KINTONE_API_TOKEN": "${KINTONE_API_TOKEN}"
      },
      "description": "Kintone back-office — fiches sources, statuts, validation humaine"
    }
  },
  "_policy": "CLI-first. MCP uniquement pour accès structurés kintone. Voir mcp/README_MCP_POLICY.md"
}
