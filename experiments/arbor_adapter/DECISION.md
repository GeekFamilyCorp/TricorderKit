# Décision — Prototype Arbor

Décision **PROTOTYPER** enregistrée dans le journal canonique : `.planning/DECISIONS.md` → **DEC-052**.

- Module concerné : `research_agent` (boucle de recherche cumulative).
- Niveau d'impact : moyen. `allowed_to_modify_core: false`.
- Rollback : supprimer `experiments/arbor_adapter/` + benchmarks liés ; garder la trace DEC-052.

Ce fichier est un simple pointeur : la source de vérité reste `.planning/DECISIONS.md`.
